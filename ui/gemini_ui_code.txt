<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gemini AI - Next Gen Interface</title>
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600&family=Roboto+Mono:wght@400&display=swap" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        :root {
            /* Gemini Color Palette */
            --bg-body: #131314;
            --bg-sidebar: #1e1f20;
            --bg-chat-bubble: #1e1f20;
            --bg-user-bubble: #282a2c;
            --bg-input: #1e1f20;
            --text-primary: #e3e3e3;
            --text-secondary: #c4c7c5;
            --accent-blue: #a8c7fa;
            --accent-gradient: linear-gradient(135deg, #4285f4, #d96570);
            --border-color: #444746;
            --transition-speed: 0.3s;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Outfit', sans-serif;
            background-color: var(--bg-body);
            color: var(--text-primary);
            height: 100vh;
            display: flex;
            overflow: hidden;
        }

        /* --- Sidebar (Web) / Drawer (Mobile) --- */
        .sidebar {
            width: 280px;
            background-color: var(--bg-body); /* In Gemini, sidebar often blends or is slightly different */
            display: flex;
            flex-direction: column;
            padding: 20px 15px;
            transition: transform var(--transition-speed) ease;
            z-index: 100;
        }

        .new-chat-btn {
            background-color: #1b1b1b;
            color: #c4c7c5;
            border: 1px solid #333;
            padding: 12px 15px;
            border-radius: 20px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 10px;
            font-family: 'Outfit', sans-serif;
            font-size: 0.9rem;
            transition: background 0.2s;
            margin-bottom: 20px;
        }
        .new-chat-btn:hover { background-color: #2b2b2b; }
        .new-chat-btn i { font-size: 1rem; color: #8e918f; }

        .history-section {
            flex: 1;
            overflow-y: auto;
        }

        .history-label {
            font-size: 0.75rem;
            font-weight: 500;
            color: var(--text-secondary);
            margin-bottom: 10px;
            padding-left: 10px;
        }

        .history-item {
            padding: 10px 15px;
            border-radius: 20px;
            cursor: pointer;
            font-size: 0.9rem;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            color: var(--text-primary);
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .history-item:hover { background-color: #282a2c; }
        .history-item i { font-size: 0.9rem; }

        .sidebar-footer {
            margin-top: auto;
            border-top: 1px solid var(--border-color);
            padding-top: 15px;
        }
        
        .footer-item {
            padding: 10px 15px;
            border-radius: 20px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 0.9rem;
        }
        .footer-item:hover { background-color: #282a2c; }

        /* --- Main Content --- */
        .main-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            position: relative;
            background-color: var(--bg-body);
            height: 100%;
        }

        /* Header (Mobile Only mainly) */
        .mobile-header {
            display: none;
            padding: 15px;
            align-items: center;
            justify-content: space-between;
            background-color: var(--bg-body);
        }
        .menu-btn { font-size: 1.2rem; cursor: pointer; color: var(--text-secondary); }
        .brand-text { font-weight: 500; font-size: 1.2rem; }

        /* Chat Area */
        .chat-container {
            flex: 1;
            overflow-y: auto;
            padding: 20px 0;
            scroll-behavior: smooth;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .welcome-screen {
            text-align: center;
            margin-top: 10vh;
            max-width: 600px;
            padding: 0 20px;
        }
        .welcome-title {
            font-size: 3.5rem;
            font-weight: 500;
            background: linear-gradient(90deg, #4285f4, #d96570);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 10px;
            letter-spacing: -1px;
        }
        .welcome-sub {
            font-size: 1.5rem;
            color: #444746;
            margin-bottom: 40px;
        }

        .suggestion-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            gap: 15px;
            width: 100%;
            max-width: 800px;
        }

        .suggestion-card {
            background-color: #1e1f20;
            padding: 15px;
            border-radius: 12px;
            cursor: pointer;
            transition: background 0.2s;
            height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }
        .suggestion-card:hover { background-color: #2c2c2e; }
        .suggestion-text { font-size: 0.9rem; color: var(--text-primary); margin-bottom: 10px; }
        .suggestion-icon { align-self: flex-end; background: #000; padding: 8px; border-radius: 50%; font-size: 0.8rem; }

        /* Messages */
        .message-wrapper {
            width: 100%;
            max-width: 800px;
            padding: 0 20px;
            margin-bottom: 25px;
            display: flex;
            gap: 15px;
        }
        
        .avatar {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
        }
        .avatar.ai { background: linear-gradient(135deg, #4285f4, #d96570); }
        .avatar.user { background: #444746; }

        .message-content {
            flex: 1;
            font-size: 1rem;
            line-height: 1.6;
            color: var(--text-primary);
        }
        .user-msg { font-weight: 400; }
        .ai-msg { font-weight: 300; }

        /* Input Area */
        .input-area {
            width: 100%;
            padding: 20px;
            display: flex;
            justify-content: center;
            background-color: var(--bg-body);
        }

        .input-container {
            width: 100%;
            max-width: 800px;
            background-color: var(--bg-input);
            border-radius: 30px;
            padding: 10px 15px;
            display: flex;
            align-items: flex-end;
            gap: 10px;
            border: 1px solid transparent; /* Mimic focus state later */
            transition: background 0.2s;
        }
        .input-container:focus-within { background-color: #282a2c; }

        .action-btn {
            background: transparent;
            border: none;
            color: var(--text-primary);
            width: 40px;
            height: 40px;
            border-radius: 50%;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: background 0.2s;
        }
        .action-btn:hover { background-color: #333; }
        .action-btn.send { color: #fff; background-color: transparent; }
        .action-btn.send.active { background-color: var(--text-primary); color: var(--bg-body); }

        textarea {
            flex: 1;
            background: transparent;
            border: none;
            color: var(--text-primary);
            font-family: 'Outfit', sans-serif;
            font-size: 1rem;
            resize: none;
            padding: 10px 0;
            max-height: 150px;
            outline: none;
        }

        .disclaimer {
            text-align: center;
            font-size: 0.75rem;
            color: #444746;
            margin-top: 10px;
            padding-bottom: 10px;
        }

        /* Loading dots */
        .loading-dot {
            width: 6px;
            height: 6px;
            background: var(--text-primary);
            border-radius: 50%;
            display: inline-block;
            opacity: 0.7;
            animation: loading-dot 1s infinite ease-in-out;
            transform-origin: center;
        }
        @keyframes loading-dot {
            0%, 60%, 100% { transform: translateY(0); opacity: 0.4; }
            30% { transform: translateY(-6px); opacity: 1; }
        }

        /* --- Mobile Responsive --- */
        @media (max-width: 768px) {
            .sidebar {
                position: fixed;
                height: 100%;
                left: -280px;
                box-shadow: 2px 0 10px rgba(0,0,0,0.5);
            }
            .sidebar.active { left: 0; }
            
            .mobile-header { display: flex; }
            
            .welcome-title { font-size: 2.5rem; }
            .welcome-sub { font-size: 1.2rem; }
            
            .suggestion-grid { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>

    <!-- Sidebar -->
    <aside class="sidebar" id="sidebar">
        <div class="mobile-close" onclick="toggleSidebar()" style="align-self: flex-end; padding: 10px; display: none;">
            <i class="fa-solid fa-xmark"></i>
        </div>
        
        <button class="new-chat-btn">
            <i class="fa-solid fa-plus"></i>
            <span>New chat</span>
        </button>

        <div class="history-section">
            <div class="history-label">Recent</div>
            <div class="history-item"><i class="fa-regular fa-message"></i> Python Code for UI</div>
            <div class="history-item"><i class="fa-regular fa-message"></i> Marketing Strategy</div>
            <div class="history-item"><i class="fa-regular fa-message"></i> React Native Tips</div>
            <div class="history-label" style="margin-top: 20px;">Previous 7 Days</div>
            <div class="history-item"><i class="fa-regular fa-message"></i> Dinner Recipes</div>
            <div class="history-item"><i class="fa-regular fa-message"></i> Trip to Japan</div>
        </div>

        <div class="sidebar-footer">
            <div class="footer-item"><i class="fa-regular fa-circle-question"></i> Help</div>
            <div class="footer-item"><i class="fa-solid fa-clock-rotate-left"></i> Activity</div>
            <div class="footer-item"><i class="fa-solid fa-gear"></i> Settings</div>
        </div>
    </aside>

    <!-- Main Content -->
    <main class="main-content">
        <!-- Mobile Header -->
        <header class="mobile-header">
            <div class="menu-btn" onclick="toggleSidebar()"><i class="fa-solid fa-bars"></i></div>
            <div class="brand-text">Gemini</div>
            <div class="avatar user" style="width: 30px; height: 30px;">U</div>
        </header>

        <!-- Chat Feed -->
        <div class="chat-container" id="chat-feed" role="log" aria-live="polite">
            <!-- Welcome Screen (Initial State) -->
            <div class="welcome-screen" id="welcome-screen">
                <h1 class="welcome-title">Hello, Amit</h1>
                <p class="welcome-sub">How can I help you today?</p>
                
                <div class="suggestion-grid">
                    <div class="suggestion-card" onclick="fillInput('Explain quantum computing in simple terms')">
                        <div class="suggestion-text">Explain quantum computing in simple terms</div>
                        <div class="suggestion-icon"><i class="fa-regular fa-lightbulb"></i></div>
                    </div>
                    <div class="suggestion-card" onclick="fillInput('Write a Python script to scrape a website')">
                        <div class="suggestion-text">Write a Python script to scrape a website</div>
                        <div class="suggestion-icon"><i class="fa-solid fa-code"></i></div>
                    </div>
                    <div class="suggestion-card" onclick="fillInput('Draft an email for a job application')">
                        <div class="suggestion-text">Draft an email for a job application</div>
                        <div class="suggestion-icon"><i class="fa-solid fa-pen-nib"></i></div>
                    </div>
                    <div class="suggestion-card" onclick="fillInput('Create a workout plan for beginners')">
                        <div class="suggestion-text">Create a workout plan for beginners</div>
                        <div class="suggestion-icon"><i class="fa-solid fa-person-running"></i></div>
                    </div>
                </div>
            </div>

            <!-- Dynamic Messages will appear here -->
        </div>

        <!-- Input Area -->
        <div class="input-area">
            <div style="width: 100%; max-width: 800px;">
                <div class="input-container">
                    <button class="action-btn" title="Upload Image"><i class="fa-regular fa-image"></i></button>
                    <button class="action-btn" title="Use Microphone"><i class="fa-solid fa-microphone"></i></button>
                    <textarea id="chat-input" rows="1" placeholder="Enter a prompt here" oninput="autoResize(this)"></textarea>
                    <button class="action-btn send" id="send-btn" onclick="sendMessage()" aria-label="Send message" type="button" disabled><i class="fa-solid fa-paper-plane"></i></button>
                </div>
                <div class="disclaimer">
                    Gemini may display inaccurate info, including about people, so double-check its responses.
                </div>
            </div>
        </div>
    </main>

    <script>
        // --- Logic ---
        const chatInput = document.getElementById('chat-input');
        const sendBtn = document.getElementById('send-btn');
        const chatFeed = document.getElementById('chat-feed');
        const welcomeScreen = document.getElementById('welcome-screen');

        function toggleSidebar() {
            document.getElementById('sidebar').classList.toggle('active');
        }

        function autoResize(textarea) {
            textarea.style.height = 'auto';
            textarea.style.height = textarea.scrollHeight + 'px';
            
            const hasText = textarea.value.trim().length > 0;
            // Toggle send button style
            if(hasText) {
                sendBtn.classList.add('active');
                sendBtn.disabled = false;
                sendBtn.setAttribute('aria-disabled', 'false');
            } else {
                sendBtn.classList.remove('active');
                sendBtn.disabled = true;
                sendBtn.setAttribute('aria-disabled', 'true');
            }
        }

        function fillInput(text) {
            chatInput.value = text;
            autoResize(chatInput);
            chatInput.focus();
        }

        function sendMessage() {
            const text = chatInput.value.trim();
            if(!text) return;

            // Hide Welcome Screen on first message
            if(welcomeScreen) welcomeScreen.style.display = 'none';

            // 1. Add User Message
            addMessage('user', text);
            
            // Clear Input
            chatInput.value = '';
            chatInput.style.height = 'auto';
            sendBtn.classList.remove('active');

            // 2. Simulate AI Typing/Thinking
            const loadingId = addLoading();

            // 3. Simulate Response (Timeout)
            setTimeout(() => {
                removeLoading(loadingId);
                addMessage('ai', "This is a simulated response. In the real app, this would be connected to the Gemini 3 Pro API backend we just configured. 

I can help you with coding, writing, or analysis.");
            }, 1500);
        }

        function addMessage(role, text) {
            const wrapper = document.createElement('div');
            wrapper.className = 'message-wrapper';
            
            let avatarHTML = '';
            if(role === 'ai') {
                avatarHTML = "<div class=\"avatar ai\"><i class=\"fa-solid fa-sparkles\" style=\"color: white; font-size: 0.8rem;"></i></div>";
            } else {
                avatarHTML = "<div class=\"avatar user\" style=\"color: white; font-size: 0.8rem;\">U</div>";
            }

            const contentHTML = `<div class="message-content ${role}-msg">${text.replace(/\n/g, '<br>')}</div>`;

            if(role === 'user') {
                // User on right or left? Gemini puts user on right usually? No, Gemini interface is left-aligned list mostly.
                // Let's stick to the list view for now.
                wrapper.innerHTML = avatarHTML + contentHTML;
            } else {
                wrapper.innerHTML = avatarHTML + contentHTML;
            }

            chatFeed.appendChild(wrapper);
            chatFeed.scrollTop = chatFeed.scrollHeight;
        }

        function addLoading() {
            const id = 'loader-' + Date.now();
            const wrapper = document.createElement('div');
            wrapper.className = 'message-wrapper';
            wrapper.id = id;

            const avatar = document.createElement('div');
            avatar.className = 'avatar ai';
            avatar.innerHTML = '<i class="fa-solid fa-sparkles" style="color: white; font-size: 0.8rem;"></i>';

            const content = document.createElement('div');
            content.className = 'message-content';
            content.style.display = 'flex';
            content.style.alignItems = 'center';
            content.style.gap = '6px';

            // three animated dots
            for (let i = 0; i < 3; i++) {
                const dot = document.createElement('span');
                dot.className = 'loading-dot';
                dot.style.animationDelay = (i * 0.12) + 's';
                content.appendChild(dot);
            }

            wrapper.appendChild(avatar);
            wrapper.appendChild(content);

            chatFeed.appendChild(wrapper);
            chatFeed.scrollTop = chatFeed.scrollHeight;
            return id;
        }

        function removeLoading(id) {
            const el = document.getElementById(id);
            if(el) el.remove();
        }

        // Keyboard: Enter to send, Shift+Enter for newline
        chatInput.addEventListener('keydown', (e) => {
            if (e.key === 'Enter' && !e.shiftKey) {
                e.preventDefault();
                sendMessage();
            }
        });

        // Close sidebar when clicking outside on mobile
        document.addEventListener('click', (e) => {
            const sidebar = document.getElementById('sidebar');
            const menuBtn = document.querySelector('.menu-btn');
            
            if (window.innerWidth <= 768 && 
                sidebar.classList.contains('active') && 
                !sidebar.contains(e.target) && 
                !menuBtn.contains(e.target)) {
                toggleSidebar();
            }
        });
    </script>
</body>
</html>